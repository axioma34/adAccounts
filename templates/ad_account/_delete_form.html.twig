<form method="post" action="{{ path('ad_account_delete', {'id': ad_account.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ ad_account.id) }}">
    <button class="btn">Delete</button>
</form>
