{% extends 'base.html.twig' %}

{% block title %}AdAccount{% endblock %}

{% block body %}
    <h1>AdAccount</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ ad_account.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ ad_account.name }}</td>
            </tr>
            <tr>
                <th>Status</th>
                <td>{{ ad_account.status ? 'Yes' : 'No' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('ad_account_index') }}">back to list</a>

    <a href="{{ path('ad_account_edit', {'id': ad_account.id}) }}">edit</a>

    {{ include('ad_account/_delete_form.html.twig') }}
{% endblock %}
